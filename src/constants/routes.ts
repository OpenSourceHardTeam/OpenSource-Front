const routes = {
  main: "/",
  bookinfo: "/bookinfo/:bookId",
  chat: "/chat",
  login: "/login",
  signup: "/signup",
  chatlist: "/chatlist",
  booklist: "/booklist",
} as const;

export default routes;
