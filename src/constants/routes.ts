const routes = {
  main: "/",
  bookinfo: "/bookinfo/:bookId",
  chat: "/chat",
  login: "/login",
  signup: "/signup",
  chatlist: "/chatlist",
  booklist: "/booklist",
  setting: "/setting",
} as const;

export default routes;
